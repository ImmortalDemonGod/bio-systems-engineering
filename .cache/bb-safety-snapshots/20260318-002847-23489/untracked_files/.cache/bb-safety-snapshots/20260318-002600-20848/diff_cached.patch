diff --git a/.github/aiv-evidence/EVIDENCE_TOOLS_INGEST_NEW_RUNS.md b/.github/aiv-evidence/EVIDENCE_TOOLS_INGEST_NEW_RUNS.md
new file mode 100644
index 0000000..caeb68b
--- /dev/null
+++ b/.github/aiv-evidence/EVIDENCE_TOOLS_INGEST_NEW_RUNS.md
@@ -0,0 +1,63 @@
+# AIV Evidence File (v1.0)
+
+**File:** `tools/ingest_new_runs.py`
+**Commit:** `b88531e`
+**Generated:** 2026-03-18T00:26:00Z
+**Protocol:** AIV v2.0 + Addendum 2.7 (Zero-Touch Mandate)
+
+---
+
+## Classification (required)
+
+```yaml
+classification:
+  risk_tier: R0
+  sod_mode: S0
+  critical_surfaces: []
+  blast_radius: "tools/ingest_new_runs.py"
+  classification_rationale: "New utility tool with dry-run mode - low blast radius, no existing logic modified"
+  classified_by: "Miguel Ingram"
+  classified_at: "2026-03-18T00:26:00Z"
+```
+
+## Claim(s)
+
+1. Script processes .gpx and .fit files from data/raw/ into biosystems pipeline format
+2. Walk detection identifies pace > 9.5 min/km OR cadence < 140 spm
+3. Regenerates real_weekly_data.json with correct weekly aggregations
+4. No existing tests were modified or deleted during this change.
+
+---
+
+## Evidence
+
+### Class E (Intent Alignment)
+
+- **Link:** [https://github.com/ImmortalDemonGod/aiv-protocol](https://github.com/ImmortalDemonGod/aiv-protocol)
+- **Requirements Verified:** AIV protocol demonstration for bio-systems-engineering project
+
+### Class B (Referential Evidence)
+
+**Scope Inventory** (SHA: [`b88531e`](https://github.com/ImmortalDemonGod/bio-systems-engineering/tree/b88531e291ef9167c48330354f59e2b7fd9d25a5))
+
+- [`tools/ingest_new_runs.py#L1-L464`](https://github.com/ImmortalDemonGod/bio-systems-engineering/blob/b88531e291ef9167c48330354f59e2b7fd9d25a5/tools/ingest_new_runs.py#L1-L464)
+
+### Class A (Execution Evidence)
+
+- Local checks skipped (--skip-checks).
+- **Skip reason:** Tool validated manually via dry-run and full processing test; no unit tests yet
+
+
+---
+
+## Verification Methodology
+
+**R0 (trivial) -- local checks skipped.**
+**Reason:** Tool validated manually via dry-run and full processing test; no unit tests yet
+Only git diff scope inventory was collected. No execution evidence.
+
+---
+
+## Summary
+
+Add automated pipeline for processing new running data files
diff --git a/tools/ingest_new_runs.py b/tools/ingest_new_runs.py
new file mode 100644
index 0000000..a1d4a93
--- /dev/null
+++ b/tools/ingest_new_runs.py
@@ -0,0 +1,464 @@
+"""
+Ingest New Runs — Bio-Systems Pipeline
+
+# mypy: ignore-errors
+=======================================
+
+Scans data/raw/ for .gpx and .fit files not yet processed and runs the
+full bio-systems pipeline on each one:
+
+  1. Parse GPX/FIT → DataFrame
+  2. Double single-leg cadence to full-stride
+  3. Recalculate pace from raw speed
+  4. Walk detection  (pace > 9.5 min/km  OR  cadence < 140 spm)
+  5. Run run_metrics() on walk-excluded data
+  6. Write  <stem>_gpx_full.csv     — all rows with walk flag
+  7. Write  <stem>_gpx_summary.csv  — run-only rows + scalar metrics broadcast
+  8. Regenerate data/real_weekly_data.json from every summary CSV
+
+Usage
+-----
+    python3.11 tools/ingest_new_runs.py                      # process data/raw/
+    python3.11 tools/ingest_new_runs.py --raw data/raw/      # explicit path
+    python3.11 tools/ingest_new_runs.py --raw /path/to/dir   # any raw directory
+    python3.11 tools/ingest_new_runs.py --dry-run            # print what would be done
+    python3.11 tools/ingest_new_runs.py --force              # reprocess all files
+"""
+
+from __future__ import annotations
+
+import argparse
+import json
+import sys
+from datetime import datetime
+from pathlib import Path
+
+import numpy as np
+import pandas as pd
+import yaml
+
+# Ensure biosystems is importable when run directly
+PROJECT_ROOT = Path(__file__).resolve().parents[1]
+sys.path.insert(0, str(PROJECT_ROOT / "src"))
+
+from biosystems.ingestion.fit import add_derived_metrics, parse_fit  # noqa: E402, type: ignore
+from biosystems.ingestion.gpx import parse_gpx  # noqa: E402, type: ignore
+from biosystems.models import HeartRateZone, ZoneConfig  # noqa: E402, type: ignore
+from biosystems.physics.metrics import run_metrics  # noqa: E402, type: ignore
+
+# ─── Zone loading ─────────────────────────────────────────────────────────────
+
+def load_zone_config(yaml_path: Path | None = None) -> ZoneConfig:
+    """Load zones_personal.yml and return a ZoneConfig ready for run_metrics()."""
+    if yaml_path is None:
+        yaml_path = PROJECT_ROOT / "data" / "zones_personal.yml"
+
+    with yaml_path.open() as f:
+        raw = yaml.safe_load(f)
+
+    raw.pop("model", None)  # strip metadata key
+
+    zones: dict[str, HeartRateZone] = {}
+    for name, data in raw.items():
+        if not isinstance(data, dict):
+            continue
+        if "bpm" not in data or "pace_min_per_km" not in data:
+            continue
+        zones[name] = HeartRateZone(
+            name=name,
+            bpm=tuple(data["bpm"]),
+            pace_min_per_km=tuple(data["pace_min_per_km"]),
+        )
+
+    return ZoneConfig(resting_hr=50, threshold_hr=186, zones=zones)
+
+
+# ─── Per-file processing ──────────────────────────────────────────────────────
+
+def _double_cadence_if_single_leg(df: pd.DataFrame) -> pd.DataFrame:
+    """Double cadence if the 90th percentile is below 100 (single-leg signal)."""
+    if "cadence" in df.columns and df["cadence"].quantile(0.90) < 100:
+        df = df.copy()
+        df["cadence"] = df["cadence"] * 2
+    return df
+
+
+def _recalculate_pace(df: pd.DataFrame) -> pd.DataFrame:
+    """Recalculate pace columns from raw speed (matches cultivation BUG #9 fix)."""
+    if "speed_mps" in df.columns:
+        df = df.copy()
+        safe_speed = df["speed_mps"].replace([0, np.inf, -np.inf], np.nan)
+        df["pace_min_per_km"] = 16.6667 / safe_speed
+        df["pace_sec_km"] = 1000 / safe_speed
+    return df
+
+
+def _add_walk_flag(df: pd.DataFrame) -> pd.DataFrame:
+    """Mark rows as walking: pace > 9.5 min/km OR cadence < 140 spm (full-stride)."""
+    df = df.copy()
+    df["is_walk"] = False
+    if "pace_min_per_km" in df.columns and "cadence" in df.columns:
+        df["is_walk"] = (df["pace_min_per_km"] > 9.5) | (df["cadence"] < 140)
+    elif "pace_min_per_km" in df.columns:
+        df["is_walk"] = df["pace_min_per_km"] > 9.5
+    return df
+
+
+def _add_compat_columns(df: pd.DataFrame) -> pd.DataFrame:
+    """Add legacy-compatibility alias columns used by downstream analysis."""
+    df = df.copy()
+    if "hr" in df.columns:
+        df["heart_rate"] = df["hr"]
+    if "lat" in df.columns:
+        df["latitude"] = df["lat"]
+        df["lat_prev"] = df["lat"].shift(1)
+    if "lon" in df.columns:
+        df["longitude"] = df["lon"]
+        df["lon_prev"] = df["lon"].shift(1)
+    if "dist" in df.columns:
+        df["distance_cumulative_km"] = df["dist"].cumsum() / 1000.0
+        df["distance_segment_m"] = df["dist"]
+
+    # Time delta
+    if df.index.name == "time":
+        t = df.reset_index()["time"]
+        df["time_prev"] = t.shift(1).values
+        df["time_delta_s"] = (t - t.shift(1)).dt.total_seconds().values
+    elif "time" in df.columns:
+        df["time_prev"] = df["time"].shift(1)
+        df["time_delta_s"] = (df["time"] - df["time_prev"]).dt.total_seconds()
+
+    return df
+
+
+def process_gpx(raw_path: Path, processed_dir: Path, zone_config: ZoneConfig, verbose: bool) -> dict | None:
+    """Parse one GPX file and write _full.csv + _summary.csv.  Returns metrics dict."""
+    stem = raw_path.stem
+    full_out = processed_dir / f"{stem}_gpx_full.csv"
+    summary_out = processed_dir / f"{stem}_gpx_summary.csv"
+
+    if verbose:
+        print("  Parsing GPX …")
+
+    df = parse_gpx(str(raw_path))
+    if df is None or df.empty:
+        print("  [ERROR] parse_gpx() returned empty DataFrame.")
+        return None
+
+    df = _double_cadence_if_single_leg(df)
+    df = _recalculate_pace(df)
+    df = _add_walk_flag(df)
+    df = _add_compat_columns(df)
+
+    # Write full CSV (all rows)
+    df.to_csv(full_out)
+    if verbose:
+        print(f"  Wrote {full_out.name}  ({len(df)} rows)")
+
+    # Compute metrics on walk-excluded data
+    metrics = run_metrics(df, zone_config)
+    if verbose:
+        print(f"  EF={metrics.efficiency_factor:.5f}  Dec={metrics.decoupling_pct:.2f}%  TSS={metrics.hr_tss:.1f}")
+
+    # Build run-only summary CSV with scalar metrics broadcast
+    core_df = df[~df["is_walk"].astype(bool)].copy().reset_index()
+    core_df["efficiency_factor"] = metrics.efficiency_factor
+    core_df["decoupling_%"] = metrics.decoupling_pct
+    core_df["hrTSS"] = metrics.hr_tss
+    core_df["distance_km"] = metrics.distance_km
+    core_df["duration_min"] = metrics.duration_min
+    core_df["avg_pace_min_per_km"] = metrics.avg_pace_min_per_km
+    core_df["avg_hr"] = metrics.avg_hr
+    if metrics.avg_cadence:
+        core_df["avg_cadence"] = metrics.avg_cadence
+    if metrics.gap_min_per_km:
+        core_df["gap_min_per_km"] = metrics.gap_min_per_km
+
+    core_df.to_csv(summary_out, index=False)
+    if verbose:
+        print(f"  Wrote {summary_out.name}  ({len(core_df)} run rows)")
+
+    return {
+        "efficiency_factor": metrics.efficiency_factor,
+        "decoupling_%": metrics.decoupling_pct,
+        "hrTSS": metrics.hr_tss,
+        "distance_km": metrics.distance_km,
+        "duration_min": metrics.duration_min,
+        "avg_pace_min_per_km": metrics.avg_pace_min_per_km,
+        "avg_hr": metrics.avg_hr,
+    }
+
+
+def process_fit(raw_path: Path, processed_dir: Path, zone_config: ZoneConfig, verbose: bool) -> dict | None:
+    """Parse one FIT file and write _fit_full.csv + _fit_summary.csv.  Returns metrics dict."""
+    stem = raw_path.stem
+    full_out = processed_dir / f"{stem}_fit_full.csv"
+    summary_out = processed_dir / f"{stem}_fit_summary.csv"
+
+    if verbose:
+        print("  Parsing FIT …")
+
+    df = parse_fit(str(raw_path))
+    if df is None or df.empty:
+        print("  [ERROR] parse_fit() returned empty DataFrame.")
+        return None
+
+    has_gps = any(c in df.columns for c in ["lat", "lon", "latitude", "longitude"])
+    if has_gps:
+        df = add_derived_metrics(df)
+
+    df = _double_cadence_if_single_leg(df)
+    df = _recalculate_pace(df)
+    df = _add_walk_flag(df)
+    df = _add_compat_columns(df)
+
+    df.to_csv(full_out, index=False)
+    if verbose:
+        print(f"  Wrote {full_out.name}  ({len(df)} rows)")
+
+    if not has_gps:
+        # HR-only FIT (e.g. Whoop) — skip metrics/summary
+        if verbose:
+            print("  No GPS data; skipping metrics.")
+        return None
+
+    metrics = run_metrics(df, zone_config)
+    if verbose:
+        print(f"  EF={metrics.efficiency_factor:.5f}  Dec={metrics.decoupling_pct:.2f}%  TSS={metrics.hr_tss:.1f}")
+
+    core_df = df[~df["is_walk"].astype(bool)].copy().reset_index(drop=True)
+    core_df["efficiency_factor"] = metrics.efficiency_factor
+    core_df["decoupling_%"] = metrics.decoupling_pct
+    core_df["hrTSS"] = metrics.hr_tss
+    core_df["distance_km"] = metrics.distance_km
+    core_df["duration_min"] = metrics.duration_min
+    core_df["avg_pace_min_per_km"] = metrics.avg_pace_min_per_km
+    core_df["avg_hr"] = metrics.avg_hr
+    if metrics.avg_cadence:
+        core_df["avg_cadence"] = metrics.avg_cadence
+    if metrics.gap_min_per_km:
+        core_df["gap_min_per_km"] = metrics.gap_min_per_km
+
+    core_df.to_csv(summary_out, index=False)
+    if verbose:
+        print(f"  Wrote {summary_out.name}  ({len(core_df)} run rows)")
+
+    return {
+        "efficiency_factor": metrics.efficiency_factor,
+        "decoupling_%": metrics.decoupling_pct,
+        "hrTSS": metrics.hr_tss,
+        "distance_km": metrics.distance_km,
+        "duration_min": metrics.duration_min,
+        "avg_pace_min_per_km": metrics.avg_pace_min_per_km,
+        "avg_hr": metrics.avg_hr,
+    }
+
+
+# ─── Weekly aggregation ───────────────────────────────────────────────────────
+
+def regenerate_weekly_json(processed_dir: Path, output_path: Path, verbose: bool) -> None:
+    """
+    Read all *_summary.csv files and regenerate real_weekly_data.json.
+
+    Only includes files whose stem starts with YYYYMMDD_ (a date prefix).
+    Non-run sessions (walk-only FIT files without EF) are skipped.
+    """
+    summary_files = sorted(processed_dir.glob("*_summary.csv"))
+
+    # Skip anything in processed/non_run/
+    summary_files = [
+        f for f in summary_files
+        if "non_run" not in str(f)
+    ]
+
+    week_data: dict[int, list[dict]] = {}
+
+    for csv_path in summary_files:
+        stem = csv_path.stem  # e.g. 20250425_191120_..._gpx_summary
+        parts = stem.split("_")
+        if not (parts[0].isdigit() and len(parts[0]) == 8):
+            continue
+
+        try:
+            dt = datetime.strptime(parts[0], "%Y%m%d")
+            iso_week = dt.isocalendar().week
+        except ValueError:
+            continue
+
+        try:
+            df = pd.read_csv(csv_path)
+        except Exception as e:
+            if verbose:
+                print(f"  [WARN] Could not read {csv_path.name}: {e}")
+            continue
+
+        if "efficiency_factor" not in df.columns:
+            continue  # HR-only or non-run file
+
+        ef = df["efficiency_factor"].iloc[0]
+        dec = df["decoupling_%"].iloc[0] if "decoupling_%" in df.columns else float("nan")
+
+        if pd.isna(ef):
+            continue
+
+        week_data.setdefault(iso_week, []).append({"ef": ef, "dec": dec})
+
+    records = []
+    for week in sorted(week_data.keys()):
+        runs = week_data[week]
+        ef_vals = [r["ef"] for r in runs]
+        dec_vals = [r["dec"] for r in runs if not pd.isna(r["dec"])]
+        records.append({
+            "week": week,
+            "ef_mean": round(float(np.mean(ef_vals)), 5),
+            "decoupling_mean": round(float(np.mean(dec_vals)), 2) if dec_vals else None,
+            "num_runs": len(runs),
+            "note": "",
+        })
+
+    with output_path.open("w") as f:
+        json.dump(records, f, indent=2)
+
+    if verbose:
+        total_runs = sum(r['num_runs'] for r in records)  # type: ignore[misc]
+        print(f"\nRegenerated {output_path.name}  ({len(records)} weeks, {total_runs} runs)")
+
+
+# ─── Main ─────────────────────────────────────────────────────────────────────
+
+def main() -> None:
+    parser = argparse.ArgumentParser(
+        description="Ingest new .gpx/.fit files from data/raw/ into the bio-systems pipeline."
+    )
+    parser.add_argument(
+        "--raw",
+        type=Path,
+        default=PROJECT_ROOT / "data" / "raw",
+        help="Directory containing raw .gpx/.fit files (default: data/raw/)",
+    )
+    parser.add_argument(
+        "--processed",
+        type=Path,
+        default=PROJECT_ROOT / "data" / "processed",
+        help="Directory for processed CSV output (default: data/processed/)",
+    )
+    parser.add_argument(
+        "--zones",
+        type=Path,
+        default=None,
+        help="Path to zones_personal.yml (default: data/zones_personal.yml)",
+    )
+    parser.add_argument(
+        "--dry-run",
+        action="store_true",
+        help="Print which files would be processed without doing anything",
+    )
+    parser.add_argument(
+        "--force",
+        action="store_true",
+        help="Reprocess files even if output CSVs already exist",
+    )
+    parser.add_argument(
+        "--no-weekly",
+        action="store_true",
+        help="Skip regenerating real_weekly_data.json after processing",
+    )
+    parser.add_argument(
+        "--verbose", "-v",
+        action="store_true",
+        help="Print detailed progress",
+    )
+    args = parser.parse_args()
+
+    raw_dir: Path = args.raw
+    processed_dir: Path = args.processed
+    weekly_json = PROJECT_ROOT / "data" / "real_weekly_data.json"
+
+    if not raw_dir.exists():
+        print(f"[ERROR] Raw directory not found: {raw_dir}")
+        sys.exit(1)
+
+    processed_dir.mkdir(parents=True, exist_ok=True)
+
+    # Collect raw files, preferring _hr_override.gpx over base .gpx when both exist
+    all_gpx = sorted(raw_dir.glob("*.gpx"))
+    override_stems = {f.stem.replace("_hr_override", "") for f in all_gpx if f.stem.endswith("_hr_override")}
+    raw_files: list[Path] = []
+    for f in all_gpx:
+        if not f.stem.endswith("_hr_override") and f.stem in override_stems:
+            continue  # base shadowed by override
+        raw_files.append(f)
+    raw_files += sorted(raw_dir.glob("*.fit"))
+    raw_files.sort()
+
+    if not raw_files:
+        print(f"No .gpx or .fit files found in {raw_dir}")
+        return
+
+    zone_config = load_zone_config(args.zones)
+    processed_count = 0
+    skipped_count = 0
+
+    for raw_path in raw_files:
+        stem = raw_path.stem
+        suffix = raw_path.suffix.lower()
+
+        # Check if already processed
+        if suffix == ".gpx":
+            marker = processed_dir / f"{stem}_gpx_full.csv"
+        else:
+            marker = processed_dir / f"{stem}_fit_full.csv"
+
+        already_done = marker.exists()
+
+        if already_done and not args.force:
+            skipped_count += 1
+            if args.verbose:
+                print(f"  [skip] {raw_path.name}")
+            continue
+
+        tag = "[dry-run]" if args.dry_run else "[process]"
+        print(f"{tag} {raw_path.name}")
+
+        if args.dry_run:
+            continue
+
+        try:
+            if suffix == ".gpx":
+                result = process_gpx(raw_path, processed_dir, zone_config, args.verbose)
+            else:
+                result = process_fit(raw_path, processed_dir, zone_config, args.verbose)
+
+            if result:
+                processed_count += 1
+                print(
+                    f"  EF={result['efficiency_factor']:.5f}  "
+                    f"Dec={result['decoupling_%']:.2f}%  "
+                    f"Dist={result['distance_km']:.2f} km"
+                )
+            else:
+                processed_count += 1  # still counts (non-GPS FIT etc.)
+
+        except Exception as exc:
+            print(f"  [ERROR] {exc}")
+            if args.verbose:
+                import traceback
+                traceback.print_exc()
+
+    if not args.dry_run:
+        print(f"\nProcessed {processed_count} files, skipped {skipped_count} already-done.")
+
+        if not args.no_weekly:
+            regenerate_weekly_json(processed_dir, weekly_json, args.verbose)
+            print(f"Updated {weekly_json.relative_to(PROJECT_ROOT)}")
+    else:
+        new_count = sum(
+            1 for f in raw_files
+            if not (processed_dir / f"{f.stem}_gpx_full.csv").exists()
+            and not (processed_dir / f"{f.stem}_fit_full.csv").exists()
+        )
+        print(f"\n{new_count} new files would be processed, {skipped_count} already done.")
+
+
+if __name__ == "__main__":
+    main()