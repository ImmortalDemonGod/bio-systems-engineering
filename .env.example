# Bio-Systems Engineering — Environment Variables
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# ── Strava API ──────────────────────────────────────────────────────────────
# Register your app at https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id_here
STRAVA_CLIENT_SECRET=your_client_secret_here

# Long-lived refresh token obtained from the initial OAuth flow.
# To get one: complete the OAuth dance once, then store the refresh_token
# returned in the token exchange response. It does not expire unless revoked.
STRAVA_REFRESH_TOKEN=your_refresh_token_here
HABITDASH_API_KEY